
FILES = convertEnsemblIDs genes GO map score train units cluster estimateSize metasom metasomgene Fusion FusionCluster FusionBreakup cutree trainQuick

build:
	for dir in $(FILES); do \
		$(MAKE) -C $$dir; \
	done
